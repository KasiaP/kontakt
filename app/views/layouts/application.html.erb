<!DOCTYPE html>
<html>
<head>
  <title>Kontakt</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
 <%= stylesheet_link_tag 'nowy', :media => 'screen' %>
<%= auto_discovery_link_tag(:atom, addressbooks_path(:atom)) %>
  </head>
  <body>
    <div class="container">
      <header>
        <%= image_tag("logo.png", :alt => "Kontakty", :class => "round") %>
        <nav class="round">
          <ul>
            <li><%= link_to "Show", @addressbook %></li>
            <li><%= link_to "Back", addressbooks_path %></li>
          </ul>
        </nav>
      </header>
      <section class="round">
        <%= yield %>
      </section>
    </div>
  </body>
</html>
